# Product Roadmap — Fellaswimming

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 80/82 tasks complete
**Current Phase:** Phase 5 — SEO, Polish, & Launch in progress (068–079, 081 done). Seed now holds 5 published articles + 5 featured testimonials. 081 security review passed (advisor: 0 critical, all WARN; all admin actions auth-check first; service-role isolated) and added migration `0004_security_hardening.sql` (search_path pin + drop redundant anon-insert policy). PENDING APPLY to remote: re-run `seed.sql` + `0004` migration (read-only MCP can't write — owner enabling RW MCP). Real coach/gallery photos still owner-supplied. Remaining: 080 (cross-device manual QA), 082 (production deploy).

---

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can run the dev server and demo what has been built.
2. **Infrastructure before features.** Phase 0 sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** By the end of Phase 1, a parent can navigate from an article to a landing-page section that answers their question. The remaining phases extend, not enable, this core experience.
4. **Test as you go.** Every task includes a verification step. Don't save testing for the end.
5. **Progressive enhancement.** Start with the simplest working version, then layer polish in Phase 5.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Mobile-first, always.** When mobile and desktop conflict on a UI decision, mobile wins. Verify every public surface on a 360x640 viewport before claiming a task complete.
8. **Bahasa Indonesia for user-facing copy, English for code & docs.** Match the project's communication standard.

---

## Phase 0: Foundation & Setup

> **Goal:** A deployed Next.js 16 app with Supabase backend, Tailwind v4 design tokens, shadcn-ui primitives, auth scaffolding, and database schema migrated. App shell renders at `https://<project>.vercel.app` with no broken routes.

**Reference sections — read these before starting this phase:**
- PRD: § 2 Technical Architecture, § 9 Design System, § 10 Auth Implementation, § 13 Dependencies & Integrations
- Vision: § 5 Design Direction > Design Tokens, § 5 Design Direction > Color Palette, § 5 Design Direction > Typography

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR titled 'Phase 0: Foundation & Setup' for review."

- [x] **TASK-001** — Initialize Next.js 16 project with App Router, TypeScript, Tailwind, ESLint, src/ directory, and import alias `@/*`.
  Files: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `src/app/layout.tsx`, `src/app/page.tsx`
  Notes: Run `npx create-next-app@latest fella-swimming` with flags `--typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`. Verify: `npm run dev` shows the default page at `localhost:3000`.

- [x] **TASK-002** — Add `vercel.json` to lock framework detection.
  Files: `vercel.json`
  Notes: Content: `{"framework":"nextjs"}`. Prevents 404 on Vercel deploys if framework detection misses. Verify: file exists and JSON valid.

- [x] **TASK-003** — Set up Tailwind v4 with CSS-first `@theme` block and remove any generated config file.
  Files: `src/app/globals.css`, `postcss.config.mjs`
  Notes: Delete `tailwind.config.ts` if generated. `globals.css` must start with `@import "tailwindcss";` followed by the `@theme` block from PRD § 9 Design System > Color Tokens. `postcss.config.mjs` uses `@tailwindcss/postcss` plugin. Verify: utility classes like `bg-primary` work on a test element.

- [x] **TASK-004** — Load fonts (General Sans via `next/font/local`, Nunito + JetBrains Mono via `next/font/google`) and wire into Tailwind tokens.
  Files: `src/app/layout.tsx`, `src/app/globals.css`, `public/fonts/GeneralSans-*.woff2`
  Notes: Download General Sans woff2 (weights 500/600/700) from `https://www.fontshare.com/fonts/general-sans` and save to `public/fonts/`. Use `next/font/local` for General Sans, `next/font/google` for Nunito (weights 400/500/600/700) and JetBrains Mono (400/500). See exact code in PRD § 9 Typography Tokens. Add CSS variables `--font-heading-loaded`, `--font-body-loaded`, `--font-mono-loaded` on `<html>`. In `globals.css`, set `--font-heading`, `--font-body`, `--font-mono` to chain through the loaded vars. Verify: inspect computed font on a heading element → "General Sans"; on body → "Nunito".

- [x] **TASK-005** — Install shadcn-ui (v4) and add baseline components. _(Note: `form` component not in base-nova v4 registry yet — defer to Phase 2 TASK-038.)_
  Files: `components.json`, `src/components/ui/*`, `src/lib/utils/cn.ts`
  Notes: `npx shadcn@latest init` (choose Default style, neutral base color, CSS variables yes). Add components: `button`, `input`, `label`, `textarea`, `select`, `dialog`, `alert-dialog`, `dropdown-menu`, `table`, `tabs`, `card`, `badge`, `switch`, `sonner` (or `toast`), `skeleton`, `separator`, `radio-group`, `form`. shadcn v4 uses `@base-ui/react`, not Radix. Verify: a `<Button variant="default">Test</Button>` renders with primary styling.

- [x] **TASK-006** — Create the .env.example and document required environment variables.
  Files: `.env.example`, `.env.local` (gitignored), `.gitignore`
  Notes: Variables per PRD § 2 Technical Architecture > Stack Integration Guide. Include `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_PROJECT_REF`, `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_WA_NUMBER`. `.env.local` is gitignored; create it locally with empty values to be filled when Supabase project exists. Verify: `git status` shows `.env.local` is untracked.

- [x] **TASK-007** — Create a Supabase project in the dashboard.
  Files: (none; external action)
  Notes: Region `ap-southeast-1` (Singapore). Note project URL, anon key, and service role key. Add to `.env.local`. Verify: project accessible at `https://supabase.com/dashboard`, status `ACTIVE_HEALTHY`.

- [x] **TASK-008** — Install Supabase client libraries and create client factories.
  Files: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `src/lib/supabase/admin.ts`
  Notes: Install `@supabase/supabase-js` and `@supabase/ssr`. `client.ts` uses `createBrowserClient`. `server.ts` uses `createServerClient` with `cookies()` from `next/headers` and `getAll`/`setAll` cookie handler per PRD § 2 Stack Integration Guide. `admin.ts` uses `createClient` from `@supabase/supabase-js` with the service role key — server-only, never imported into a Client Component. Verify: `import` of each works in a server component without TS errors.

- [x] **TASK-009** — Create the Next 16 `proxy.ts` to refresh sessions.
  Files: `src/proxy.ts`
  Notes: Follow the example in PRD § 10 Auth Implementation > Protected Routes. Use `createServerClient` with `getAll`/`setAll` cookie pattern. Call `supabase.auth.getUser()` to refresh. Export `config.matcher` excluding `_next/static`, `_next/image`, `favicon.ico`. Note: Next 16 uses `proxy.ts` (not `middleware.ts`) with function `proxy()`. Verify: dev server starts without warnings; navigating any route still works.

- [x] **TASK-010** — Write Supabase migration 0001: initial schema (registrations, articles, categories, testimonials, profiles, enums, triggers). _(File written; not yet applied — pending TASK-007.)_
  Files: `supabase/migrations/0001_init_schema.sql`
  Notes: Copy the schema from PRD § 3 Data Model > Entity Definitions. Include all `CREATE TYPE`, `CREATE TABLE`, `CREATE FUNCTION`, `CREATE TRIGGER`, `CREATE SEQUENCE` statements. Indexes from § 3 Indexes go in the same file. Apply via Supabase Studio SQL editor or `supabase db push`. Verify: query `\dt` in SQL editor lists all 5 tables.

- [x] **TASK-011** — Write Supabase migration 0002: RLS policies. _(File written; not yet applied — pending TASK-007.)_
  Files: `supabase/migrations/0002_rls_policies.sql`
  Notes: Enable RLS on all tables. Policies: (a) anon can `INSERT` into `registrations` as defense-in-depth. (b) anon can `SELECT` from `articles` where `status = 'published'`. (c) anon can `SELECT` from `testimonials` where `status = 'published' AND featured = true`. (d) anon can `SELECT` from `categories`. (e) authenticated users (admin) can do everything on all tables. Verify: from Supabase Studio with anon key, can SELECT published articles but not draft; can INSERT to registrations.

- [x] **TASK-012** — Write Supabase migration 0003: storage buckets and bucket policies. _(File written; not yet applied — pending TASK-007.)_
  Files: `supabase/migrations/0003_storage_buckets.sql`
  Notes: Create buckets `article-images`, `article-covers`, `testimonial-photos` with `public = true`. Add policy: authenticated users can `INSERT`/`UPDATE`/`DELETE`. Anon `SELECT` allowed via public flag. Verify: from Supabase Studio, buckets visible in Storage tab.

- [x] **TASK-013** — Create handwritten TypeScript database types.
  Files: `src/types/database.types.ts`
  Notes: Until Docker is available for `supabase gen types typescript`, write the types by hand. Mirror the schema from migration 0001. Include enum types and Row/Insert/Update for each table. Add a script `db:types` to `package.json` for future regeneration. Verify: `import type { Database } from "@/types/database.types"` works in a server component.

- [x] **TASK-014** — Set up TanStack Query provider and React Query DevTools.
  Files: `src/providers/QueryProvider.tsx`, `src/app/layout.tsx`
  Notes: Install `@tanstack/react-query` and `@tanstack/react-query-devtools`. `QueryProvider` is a Client Component wrapping children in `<QueryClientProvider>`. Add `<ReactQueryDevtools />` only in dev. Wrap children in `src/app/layout.tsx`. Default `staleTime: 30_000`, `gcTime: 5 * 60 * 1000`. Verify: dev server shows the React Query DevTools toggle in bottom-right.

- [x] **TASK-015** — Set up Sonner toast provider (or shadcn toast).
  Files: `src/providers/ToastProvider.tsx` (or include in layout), `src/app/layout.tsx`
  Notes: Use Sonner (preferred by shadcn-ui v4). Add `<Toaster />` to root layout. Verify: calling `toast("Hello")` from a test client component renders a toast.

- [x] **TASK-016** — Configure design token tests (sanity check).
  Files: `src/app/page.tsx` (temporary)
  Notes: On the homepage, render a swatch of tokens: primary, secondary, accent, foreground, surface, success, warning, error. Use h1-h5 with each font. This verifies all tokens propagate correctly through Tailwind v4 and `next/font`. Verify: dev server shows all colors and fonts correctly; remove the swatch before phase PR is opened.

- [x] **TASK-017** — Deploy Phase 0 to Vercel.
  Files: (none; external action)
  Notes: Connect GitHub repo to Vercel. Add all environment variables in Vercel Project Settings → Environment Variables (Production + Preview). Trigger deploy via git push to `main`. Verify: production URL serves the homepage with the design swatch (or empty homepage if swatch removed).

---

## Phase 1: Public Landing & Articles

> **Goal:** A fully functional public landing page with all sections rendering, and a working articles surface (index + detail) seeded with 2 articles. By the end of this phase, the magic moment is demonstrable: a visitor reads an article, clicks its contextual CTA, and lands on the relevant landing-page anchor section.

**Reference sections — read these before starting this phase:**
- PRD: § 5 User Stories > Epic: Public Discovery & Education, § 6 Functional Requirements (FR-001 through FR-009), § 8 UI/UX Requirements > Landing / Artikel Index / Artikel Detail, § 9 Design System
- Vision: § 3 Product Strategy > Magic Moment Design, § 3 Product Strategy > Core User Flows, § 4 Brand Strategy > Voice & Tone Guide, § 4 Brand Strategy > Messaging Framework, § 5 Design Direction

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/public-landing-and-articles`, commit, push, and open a PR titled 'Phase 1: Public Landing & Articles' for review."

- [x] **TASK-018** — Build the public route group with shared layout (header, footer).
  Files: `src/app/(public)/layout.tsx`, `src/components/public/Header.tsx`, `src/components/public/Footer.tsx`, `src/components/shared/Logo.tsx`
  Notes: Header: logo (left), nav links (Kelas, Pelatih, Artikel, Lokasi, "Daftar" primary CTA). Sticky on scroll with subtle shadow on scroll. Footer per FR-007. Mobile nav: hamburger opening a slide-in drawer (shadcn `Sheet` or custom). Verify: any page under `(public)` shows header and footer.

- [x] **TASK-019** — Create hardcoded coach data and gallery data.
  Files: `src/data/coaches.ts`, `src/data/gallery.ts`, `src/data/classes.ts`
  Notes: `coaches.ts` exports an array `coaches` with fields per FR-003. Use 3-5 placeholder coaches with placeholder image URLs (use `/images/coaches/placeholder.jpg` until owner provides real photos). `gallery.ts` exports an array of 8-12 photos. `classes.ts` exports the 3 class types with pricing placeholders. All typed strictly. Verify: import in a component without TS errors.

- [x] **TASK-020** — Build the Hero section (FR-001).
  Files: `src/components/public/landing/HeroSection.tsx`
  Notes: Tagline from vision § Messaging Framework. Primary CTA `Daftar Sekarang` linking to `/daftar`. Secondary CTA `Pelajari Lebih Lanjut` anchoring to `#jenis-kelas`. Optional background bubble motif (`src/components/shared/ThemeBubbles.tsx`) at 5% opacity. Verify on 360x640: tagline and CTA visible above the fold.

- [x] **TASK-021** — Build the Tentang Singkat section.
  Files: `src/components/public/landing/AboutSection.tsx`
  Notes: 2-3 short paragraphs introducing Fellaswimming. Use brand voice per vision § Voice & Tone Guide. Include "5 tahun sudah ngajarin anak-anak Sidoarjo berenang dengan aman dan terstruktur." Anchor `#tentang`. Verify: reads natural and matches brand voice.

- [x] **TASK-022** — Build the Jenis Kelas section (FR-002).
  Files: `src/components/public/landing/KelasSection.tsx`
  Notes: 3 cards from `src/data/classes.ts`. Each card: name, short description, price range, ideal-for note, soft CTA "Daftar Privat" / "Daftar Semi" / "Daftar Grup" → `/daftar?kelas=privat` etc. Section anchor `#jenis-kelas`. Mobile 1-col, tablet 2-col, desktop 3-col. Verify: cards render with consistent height; price formatted as "Rp X.XXX.XXX / sesi".

- [x] **TASK-023** — Build the Pelatih section (FR-003).
  Files: `src/components/public/landing/PelatihSection.tsx`
  Notes: Map over `src/data/coaches.ts`. Each card: photo (Next.js `<Image>`), name, cert, years exp, short bio. Anchor `#pelatih`. Mobile 1-col, desktop 3-col. Verify: photos load with `loading="lazy"` (except first one which should be eager).

- [x] **TASK-024** — Build the Galeri section (FR-004).
  Files: `src/components/public/landing/GallerySection.tsx`
  Notes: Map over `src/data/gallery.ts`. Grid: 2-col mobile, 3-col tablet, 4-col desktop. All `<Image>` with lazy load + alt text. Optional lightbox deferred to Phase 5. Section anchor `#galeri`. Verify: photos render in grid with consistent gap and aspect ratio.

- [x] **TASK-025** — Build the Testimoni section (FR-005, server-fetched).
  Files: `src/components/public/landing/TestimoniSection.tsx`, `src/lib/queries/testimonials.ts`
  Notes: `getFeaturedTestimonials()` query in `src/lib/queries/testimonials.ts` (Server Component, `revalidate: 60`). Component renders carousel on mobile (swipeable; can use `embla-carousel-react` or pure CSS scroll-snap) and grid on desktop. Empty state: section is hidden if no testimonials. Each card: name, role, 5-star rating render, text. Anchor `#testimoni`. Verify: with 0 testimonials, section is hidden; with seeded data, renders correctly.

- [x] **TASK-026** — Seed 3 testimonials in the database for development and demo.
  Files: `supabase/seed.sql`
  Notes: INSERT 3 testimonials with `status='published'` and `featured=true`. Run via Supabase Studio SQL editor. Verify: Testimoni section renders all 3 in production.

- [x] **TASK-027** — Build the Lokasi section with Google Maps iframe (FR-006).
  Files: `src/components/public/landing/LokasiSection.tsx`
  Notes: Address text, landmark note, parking note. Iframe from Google Maps share embed (no API key). Use Fellaswimming's actual location share URL. Iframe `loading="lazy"`, responsive height. "Buka di Google Maps" link below in new tab. Anchor `#lokasi`. Verify: map renders with the correct location pin.

- [x] **TASK-028** — Build the Footer CTA section (FR-007).
  Files: `src/components/public/landing/FooterCTA.tsx`
  Notes: Tagline reinforcement + `Daftar Sekarang` button. Lighter accent background to visually separate from main content. Verify: button click navigates to `/daftar`.

- [x] **TASK-029** — Compose the landing page using all sections.
  Files: `src/app/(public)/page.tsx`
  Notes: Server Component. Render: HeroSection → AboutSection → KelasSection → PelatihSection → GallerySection → TestimoniSection → LokasiSection → FooterCTA. Add `<main>` semantic wrapper. Apply `revalidate: 60` for testimonials freshness. Verify on mobile: smooth scroll, all sections render in order, no horizontal scroll, anchor links work.

- [x] **TASK-030** — Build the article query layer.
  Files: `src/lib/queries/articles.ts`
  Notes: `getPublishedArticles({ limit, offset })` returns paginated articles. `getArticleBySlug(slug)` returns single article or null. Both use `createServerClient`. Add type returns from `src/types/database.types.ts`. Verify: call from a test page logs article rows.

- [x] **TASK-031** — Build the articles index page (FR-008).
  Files: `src/app/(public)/artikel/page.tsx`, `src/components/public/article/ArticleList.tsx`, `src/components/public/article/ArticleCard.tsx`
  Notes: Server Component fetches `getPublishedArticles({ limit: 12, offset })`. Page header: "Artikel Fellaswimming" + intro paragraph. ArticleCard shows cover, title, excerpt, publish date (in Bahasa Indonesia format), reading time. Empty state: "Artikel baru sedang dipersiapkan." Pagination via `?page=N` query param. `revalidate: 60`. Verify on /artikel: cards render correctly.

- [x] **TASK-032** — Build the article renderer for sanitized Tiptap HTML.
  Files: `src/components/public/article/ArticleRenderer.tsx`, `src/lib/utils/sanitize.ts`
  Notes: Use `rehype-sanitize` or `dompurify` to sanitize on render. Style prose with `@tailwindcss/typography` plugin or custom prose styles. Max-width 65ch. Apply rounded image style for inline images. Verify: render a test HTML string with `<img>`, `<a>`, headings, paragraphs.

- [x] **TASK-033** — Build the article detail page (FR-009).
  Files: `src/app/(public)/artikel/[slug]/page.tsx`
  Notes: Server Component. Fetch via `getArticleBySlug`. 404 if not found or status != published. Layout: cover image full-bleed on mobile / contained on desktop, title, meta (author, date, reading time), `<ArticleRenderer />` with `content_html`, contextual CTA block at end ("Lihat kelas Fellaswimming yang fit untuk anak Anda" → `/#jenis-kelas`). `generateMetadata` for SEO (FR-022). `revalidate: 300`. Verify: navigate to seeded article slug, all parts render; navigate to fake slug, 404 shown.

- [x] **TASK-034** — Seed 2 articles in the database for Phase 1 demo.
  Files: `supabase/seed.sql`
  Notes: INSERT 2 articles with status='published'. Use realistic titles like "Usia Ideal Anak Mulai Les Renang" and "Bedanya Kelas Privat, Semi, dan Grup". Each with cover image (use a Supabase Storage public URL or Unsplash placeholder for now). Both articles end with a contextual CTA in their content_html linking to `/#jenis-kelas`. Verify: /artikel index shows both; /artikel/[slug] of each renders correctly; clicking the in-article CTA navigates to `/#jenis-kelas` and scrolls to that section.

---

## Phase 2: Registration Flow

> **Goal:** A fully working 3-step registration wizard at `/daftar` that submits leads to the database with the `display_id` correctly generated. Visitors can complete the entire form on mobile in under 3 minutes.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model > Entity Definitions (registrations table), § 4 API Specification > Public mutations (submitRegistration), § 5 User Stories > Epic: Registration, § 6 Functional Requirements (FR-010, FR-011), § 8 UI/UX Requirements > Daftar
- Vision: § 4 Brand Strategy > Voice & Tone Guide (for copy)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/registration-flow`, commit, push, and open a PR titled 'Phase 2: Registration Flow' for review."

- [x] **TASK-035** — Create the registration Zod schema.
  Files: `src/lib/validation/registration.schema.ts`
  Notes: Schema matching `RegistrationInput` from PRD § 4 API Specification. Export both the full schema and per-step schemas (`StepStudentSchema`, `StepClassSchema`, `StepContactSchema`) for per-step validation. Verify: `schema.safeParse(testData)` works.

- [x] **TASK-036** — Build the `submitRegistration` Server Action.
  Files: `src/lib/actions/registration.ts`
  Notes: Copy from PRD § 4. Use `createAdminClient` (service role) to bypass anon RETURNING issue. Validate input with Zod first. Return typed result. Verify: call from a test page; lead appears in Supabase `registrations` table with auto-generated `display_id` (format `PRIV-2026-0001` etc).

- [x] **TASK-037** — Build the WhatsApp number normalization util.
  Files: `src/lib/utils/wa.ts`
  Notes: `normalizeWhatsApp(input: string): string | null` — strips spaces, +, dashes; converts leading `0` to `62`; validates final format `^62\d{9,13}$`. Returns null if invalid. `buildWhatsAppLink(phone, message)` → `wa.me/<phone>?text=<encoded message>`. Verify: unit-test cases like `08123456789` → `628123456789`, `+62 812-3456-789` → `628123456789`.

- [x] **TASK-038** — Build the Step 1 (Info Anak) component.
  Files: `src/components/public/registration/StepStudent.tsx`
  Notes: react-hook-form with zodResolver on `StepStudentSchema`. Fields: student_name (input), student_age (number input), student_gender (radio group), student_experience (radio group with descriptions per option). Per FR-010, validate before allowing advance. Verify: invalid fields block Continue; valid fields allow Continue.

- [x] **TASK-039** — Build the Step 2 (Kelas) component.
  Files: `src/components/public/registration/StepClass.tsx`
  Notes: Fields: preferred_class_type (radio group with class type cards showing name + brief), preferred_schedule (input or select with common slots), preferred_location (input or select). Pre-select `preferred_class_type` from URL query `?kelas=privat` if present (deep link from KelasSection CTAs). Verify: deep-linked class type pre-selects; can advance.

- [x] **TASK-040** — Build the Step 3 (Kontak) component.
  Files: `src/components/public/registration/StepContact.tsx`
  Notes: Fields: parent_name (input), parent_whatsapp (input, mask `+62xxxxxxxxxx` or guide text), parent_email (optional input), notes (textarea). On submit, normalize phone via `normalizeWhatsApp`. Verify: invalid phone shows inline error; valid submits.

- [x] **TASK-041** — Build the RegistrationWizard orchestrator.
  Files: `src/components/public/registration/RegistrationWizard.tsx`
  Notes: Client Component managing step state and form state across steps. Render step progress indicator (1/3, 2/3, 3/3) at top. Render current step. Back/Continue buttons. Final submit posts to `submitRegistration`. On success, render `<ConfirmationPanel />` with `display_id`. On error, toast with retry. Verify: full happy path completes on mobile.

- [x] **TASK-042** — Build the ConfirmationPanel.
  Files: `src/components/public/registration/ConfirmationPanel.tsx`
  Notes: Display: success icon, "Pendaftaran terkirim!", `display_id` prominent and copyable, next-steps copy ("Tim Fellaswimming akan WhatsApp kamu dalam 1x24 jam"), secondary CTA "Baca Artikel" → `/artikel`. Verify: panel renders after a successful submit.

- [x] **TASK-043** — Wire RegistrationWizard into the `/daftar` page.
  Files: `src/app/(public)/daftar/page.tsx`
  Notes: Page is a Server Component that renders the Client `<RegistrationWizard />`. Add page metadata (title "Daftar — Fellaswimming"). Layout: centered, max-width 480px. Verify on mobile: completing entire form works end-to-end, lead in DB.

- [x] **TASK-044** — Add 'kelas' query param deep-link handling and verify magic moment Path 2.
  Files: `src/components/public/landing/KelasSection.tsx`
  Notes: Each class card's CTA should link to `/daftar?kelas={privat|semi_privat|grup}`. Verify: clicking a class CTA on landing pre-selects that class in Step 2.

---

## Phase 3: Admin Auth & Lead Management

> **Goal:** A working admin dashboard at `/admin` with email+password auth, a home overview, and full pendaftaran management. Staff can log in, see today's leads, open a modal to update status, send WhatsApp follow-up, and add internal notes.

**Reference sections — read these before starting this phase:**
- PRD: § 4 API Specification > Admin mutations, § 5 User Stories > Epic: Admin Authentication, § 5 User Stories > Epic: Admin Lead Management, § 6 Functional Requirements (FR-012 through FR-016), § 8 UI/UX Requirements > Login / Lupa Password / Reset Password / Admin Home / Pendaftaran List / Pendaftaran Modal, § 10 Auth Implementation
- Vision: § 3 Product Strategy > Product Principles (admin is a serious surface)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/admin-auth-and-leads`, commit, push, and open a PR titled 'Phase 3: Admin Auth & Lead Management' for review."

- [x] **TASK-045** — Configure Supabase Auth and provision the first admin user.
  Files: (none; external action)
  Notes: In Supabase Studio: enable Email provider, disable signups. Customize "Reset Password" email template in Bahasa Indonesia. Set Site URL to `NEXT_PUBLIC_SITE_URL`. Add `/reset-password` to Additional Redirect URLs. Provision admin@fellaswimming.com via Studio → Authentication → Users → Invite User. Verify: invited user receives confirmation email; can set password.

- [x] **TASK-046** — Build auth Server Actions.
  Files: `src/lib/actions/auth.ts`
  Notes: `signIn(email, password)`, `signOut()`, `requestPasswordReset(email)`, `resetPassword(newPassword, accessToken)`. All use `createServerClient`. `signIn` redirects to `/admin` on success. `signOut` redirects to `/login`. `requestPasswordReset` always returns success even if email doesn't exist (per FR-013). Verify: each action callable from a form.

- [x] **TASK-047** — Build the auth route group and login page.
  Files: `src/app/(auth)/layout.tsx`, `src/app/(auth)/login/page.tsx`, `src/components/auth/LoginForm.tsx`
  Notes: `(auth)` layout: centered card, no nav. Login form: email, password, "Lupa password?" link. If user already authenticated, redirect to `/admin` in the page Server Component. Generic error message. Verify: login with provisioned credentials → redirected to `/admin`; wrong password → "Email atau password salah".

- [x] **TASK-048** — Build the forgot password and reset password pages.
  Files: `src/app/(auth)/lupa-password/page.tsx`, `src/app/(auth)/reset-password/page.tsx`
  Notes: Per FR-013 and UI/UX Requirements > Lupa Password / Reset Password. Verify: request reset → email arrives; click link → reset password page works; new password → redirected to /admin.

- [x] **TASK-049** — Build the admin route group with sidebar layout.
  Files: `src/app/(admin)/layout.tsx`, `src/components/admin/AdminSidebar.tsx`
  Notes: Server Component layout checks `supabase.auth.getUser()` → redirect to /login if null. Sidebar: logo, nav items (Beranda, Pendaftaran, Artikel, Testimoni), logout button at bottom. Active nav state. Desktop-only layout (mobile breakpoint shows "Admin desktop only" message). Verify: navigating /admin without auth redirects to /login; with auth shows sidebar.

- [x] **TASK-050** — Build the admin overview Server Component queries.
  Files: `src/lib/queries/registrations.ts`
  Notes: `getOverviewStats()` returns `{ thisMonth, newToday, pendingFollowUp }`. `getRecentLeads(limit = 5)` returns 5 most recent. `getRegistrationsList({ status?, search?, page = 1, pageSize = 50 })` returns paginated list with total count. All use `createServerClient`. Verify: queries return expected shape from existing leads.

- [x] **TASK-051** — Build the admin home dashboard (FR-014).
  Files: `src/app/(admin)/admin/page.tsx`, `src/components/admin/overview/StatCard.tsx`, `src/components/admin/overview/RecentLeadsTable.tsx`
  Notes: Server Component fetches stats and recent leads. StatCard: icon + label + count. RecentLeadsTable: 5 rows with display_id, parent_name, status badge, created_at relative ("2 jam lalu"). Click row → open lead modal (state shared via parent client wrapper). Quick links to artikel and testimoni. Verify: counts match DB; clicking row opens modal.

- [x] **TASK-052** — Build the `updateRegistrationStatus` Server Action with revalidation.
  Files: `src/lib/actions/registration.ts` (extend)
  Notes: Action takes id, status, optional internal_notes. Updates row; if status moves to `dihubungi` for the first time, set `contacted_at = NOW()`. Call `revalidatePath("/admin/pendaftaran")` and `revalidatePath("/admin")`. Verify: action mutates row and revalidates.

- [x] **TASK-053** — Build the Pendaftaran list page with filter + search (FR-015).
  Files: `src/app/(admin)/admin/pendaftaran/page.tsx`, `src/components/admin/pendaftaran/PendaftaranTable.tsx`, `src/components/admin/pendaftaran/StatusBadge.tsx`
  Notes: Server Component prefetches first page into TanStack Query. Client component renders filter tabs (All, Baru, Dihubungi, Trial, Daftar, Tidak Lanjut), search input (debounced 300ms via useDebouncedValue hook), and table. Pagination at bottom. Click row → open modal. Verify on /admin/pendaftaran: filter changes refresh list; search filters by name/phone; pagination works.

- [x] **TASK-054** — Build the Pendaftaran modal with status edit + WhatsApp button + notes (FR-016).
  Files: `src/components/admin/pendaftaran/PendaftaranModal.tsx`, `src/hooks/usePendaftaranMutation.ts`
  Notes: shadcn Dialog. Renders full lead detail (read-only). Editable status dropdown, internal_notes textarea, WhatsApp button (`buildWhatsAppLink(parent_whatsapp, template)` opens new tab; template from FR-016). Save button uses TanStack Query `useMutation` wrapping `updateRegistrationStatus` with optimistic update + rollback on error. Verify: save closes modal, list row reflects new status; rollback on simulated error.

- [x] **TASK-055** — Add the logout flow.
  Files: `src/components/admin/AdminSidebar.tsx` (extend)
  Notes: Logout button calls `signOut` Server Action via a Client form. On success, redirect to `/login`. Verify: clicking logout → redirected to /login; cannot return to /admin without re-auth.

---

## Phase 4: Content CMS (Artikel + Testimoni)

> **Goal:** Admin can create, edit, publish, and delete articles via a Tiptap rich-text editor with image upload to Supabase Storage. Admin can manage testimonials with drag-reorder, featured toggle, and status management.

**Reference sections — read these before starting this phase:**
- PRD: § 3 Data Model (articles, testimonials, categories), § 4 API Specification > Admin mutations (articles, testimonials, image uploads), § 5 User Stories > Epic: Admin Article CMS / Epic: Admin Testimoni CMS, § 6 Functional Requirements (FR-017 through FR-021), § 8 UI/UX Requirements > Artikel List / Artikel Editor / Testimoni List / Testimoni Modal, § 12 Edge Cases (Article CMS, Testimoni CMS)
- Vision: § 4 Brand Strategy > Voice & Tone Guide (for editor placeholders)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/content-cms`, commit, push, and open a PR titled 'Phase 4: Content CMS' for review."

- [x] **TASK-056** — Build image upload Server Actions for all three buckets (FR-019).
  Files: `src/lib/actions/uploads.ts`
  Notes: `uploadArticleImage(file)`, `uploadArticleCover(file)`, `uploadTestimonialPhoto(file)`. Validate MIME (image/jpeg, image/png, image/webp) and size (5MB max) before upload. Use service-role client for upload. File name: `crypto.randomUUID() + extension`. Return `{ success, url? , error? }`. Verify: upload a JPG → public URL accessible; upload a 10MB file → rejected; upload a PDF → rejected.

- [x] **TASK-057** — Build the Tiptap editor component with image upload integration.
  Files: `src/components/admin/artikel/TiptapEditor.tsx`, `src/components/admin/artikel/EditorToolbar.tsx`
  Notes: useEditor with `immediatelyRender: false`, extensions: StarterKit, Image, Link, Placeholder ("Tulis artikelmu di sini..."), Youtube. Toolbar: bold, italic, heading (h2/h3), bullet list, ordered list, blockquote, link (modal to enter URL), image (file picker → uploadArticleImage → insert), youtube (URL input). Use shadcn Button + DropdownMenu for toolbar. Verify: editor renders without hydration error; toolbar actions all work; image insertion uploads to Supabase.

- [x] **TASK-058** — Build article CRUD Server Actions.
  Files: `src/lib/actions/articles.ts`
  Notes: `createArticle(input)`, `updateArticle(id, input)`, `deleteArticle(id)`, `publishArticle(id)`, `unpublishArticle(id)`. Use `createServerClient`, auth-check first. `publishArticle` sets `published_at = NOW()` if not set, then revalidates `/artikel` and `/artikel/[slug]`. Sanitize content_html on save. Verify: each action mutates DB correctly.

- [x] **TASK-059** — Build the article validation schema.
  Files: `src/lib/validation/article.schema.ts`
  Notes: Zod schema for article input. Required: title, excerpt, content (Tiptap JSON), content_html, cover_image_url, cover_image_alt. Optional: slug (auto-gen if blank via `slugify`), category_id, seo_title, seo_description, reading_time_minutes (auto-calc from content length if blank). Verify: schema validates a sample input.

- [x] **TASK-060** — Build the slug generation util.
  Files: `src/lib/utils/slug.ts`
  Notes: `slugify(text: string): string` — lowercase, replace spaces with hyphens, strip diacritics, strip non-alphanumeric. Test cases: "Usia Ideal Anak" → "usia-ideal-anak", "Bedanya Privat & Grup" → "bedanya-privat-grup". Verify: unit-test cases pass.

- [x] **TASK-061** — Build the article list page (FR-017).
  Files: `src/app/(admin)/admin/artikel/page.tsx`, `src/components/admin/artikel/ArtikelTable.tsx`
  Notes: Server Component prefetches articles into TanStack Query. Filter tabs (All, Draft, Published). Search by title (debounced). Buttons per row: Edit (link), Delete (confirm dialog → deleteArticle), Toggle Publish (publishArticle / unpublishArticle). "New Article" button → /admin/artikel/new. Verify: all actions work and revalidate the list.

- [x] **TASK-062** — Build the article editor page (new and edit) (FR-018).
  Files: `src/app/(admin)/admin/artikel/new/page.tsx`, `src/app/(admin)/admin/artikel/[id]/edit/page.tsx`, `src/components/admin/artikel/ArticleEditor.tsx`
  Notes: Same `ArticleEditor` Client Component for both. Edit mode hydrates form with existing article. Fields: title, slug (auto-fill from title with manual override), excerpt, cover_image (upload via uploadArticleCover), category select, SEO title, SEO description, then full-width TiptapEditor. Save Draft / Publish buttons. react-hook-form + zodResolver. Verify: create a new article, save as draft, edit it, publish it → appears on /artikel.

- [x] **TASK-063** — Build the testimonial validation schema.
  Files: `src/lib/validation/testimonial.schema.ts`
  Notes: Zod schema: name (required), role (optional), rating (1-5 int), text (required), photo_url (optional), featured (boolean), status (enum), display_order (int, defaults to max+1 on create). Verify: schema validates samples.

- [x] **TASK-064** — Build testimonial CRUD Server Actions including reorder.
  Files: `src/lib/actions/testimonials.ts`
  Notes: `createTestimonial`, `updateTestimonial`, `deleteTestimonial`, `toggleFeatured(id)`, `reorderTestimonials(orderedIds: string[])`. `reorderTestimonials` updates `display_order` per id. All revalidate `/` (landing). Verify: actions work; landing testimonials reflect changes.

- [x] **TASK-065** — Build the testimonial list page with drag-reorder (FR-020).
  Files: `src/app/(admin)/admin/testimoni/page.tsx`, `src/components/admin/testimoni/TestimoniTable.tsx`, `src/components/admin/testimoni/SortableRow.tsx`
  Notes: Use `@dnd-kit/sortable` for drag-reorder. SortableRow with drag handle (lucide GripVertical icon). On drop, call `reorderTestimonials`. Inline featured toggle (Switch). Buttons: New, Edit (open TestimoniModal), Delete (confirm dialog). Verify: drag-reorder persists; featured toggle updates landing within 30s.

- [x] **TASK-066** — Build the testimonial modal for add/edit (FR-021).
  Files: `src/components/admin/testimoni/TestimoniModal.tsx`
  Notes: shadcn Dialog. Form: name, role, rating (5-star picker as 5 buttons), text textarea, photo upload (optional, via uploadTestimonialPhoto), featured Switch, status Select. Save closes modal and refreshes list. Verify: full happy path of create + edit.

- [x] **TASK-067** — Wire admin Quick Links from home dashboard to artikel and testimoni.
  Files: `src/app/(admin)/admin/page.tsx` (verify links)
  Notes: Ensure Quick Links from FR-014 navigate to /admin/artikel and /admin/testimoni. Verify: links work.

---

## Phase 5: SEO, Polish, & Launch

> **Goal:** Production-ready Fellaswimming.com. SEO metadata complete, sitemap and robots configured, all edge cases handled, accessibility audit passes, performance budgets met, content seed in place for soft launch.

**Reference sections — read these before starting this phase:**
- PRD: § 6 Functional Requirements (FR-022, FR-023), § 7 Non-Functional Requirements, § 12 Edge Cases & Error Handling, § 15 Open Questions
- Vision: § 5 Design Direction > Accessibility Commitments, § 5 Design Direction > Motion & Interaction

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/seo-polish-launch`, commit, push, and open a PR titled 'Phase 5: SEO, Polish, & Launch' for review."

- [x] **TASK-068** — Add SEO metadata to all public pages (FR-022).
  Files: `src/app/(public)/page.tsx`, `src/app/(public)/daftar/page.tsx`, `src/app/(public)/artikel/page.tsx`, `src/app/(public)/artikel/[slug]/page.tsx`, `src/lib/constants/seo.ts`
  Notes: Each page exports `metadata` (or `generateMetadata` for dynamic). Centralize defaults in `src/lib/constants/seo.ts`. Landing: title = "Fellaswimming — Sekolah Renang Sidoarjo", description = tagline, og:image = `/images/og-default.png`. Articles use cover_image_url for og:image. Add canonical URL via `NEXT_PUBLIC_SITE_URL + pathname`. Verify: view page source on each public page → meta tags present.

- [x] **TASK-069** — Add structured data (JSON-LD) for articles.
  Files: `src/app/(public)/artikel/[slug]/page.tsx`
  Notes: Inline a `<script type="application/ld+json">` with Article schema: headline, image, datePublished, dateModified, author. Use Next.js Script component or inline in metadata. Verify: Google Rich Results Test tool reports valid Article schema.

- [x] **TASK-070** — Generate dynamic sitemap and robots.txt (FR-023).
  Files: `src/app/sitemap.ts`, `src/app/robots.ts`
  Notes: sitemap.ts: include `/`, `/daftar`, `/artikel`, and all published article slugs with `lastmod` = `updated_at`. robots.ts: allow all crawlers, disallow `/admin`, `/login`, `/lupa-password`, `/reset-password`, `/api`. Verify: visit /sitemap.xml and /robots.txt → both render correctly.

- [x] **TASK-071** — Add Vercel Analytics.
  Files: `src/app/layout.tsx`
  Notes: Install `@vercel/analytics`. Add `<Analytics />` to root layout. Verify: after deploy, Vercel dashboard → Analytics shows page views.

- [x] **TASK-072** — Apply edge-case error handling for the registration form.
  Files: `src/components/public/registration/RegistrationWizard.tsx`, `src/lib/actions/registration.ts`
  Notes: Per § 12 Registration Form table. Normalize WA number with leading 0 → 62. Disable submit button after first click to prevent double-submit. On network error, toast with "Gagal kirim. Coba lagi." + retry button. Verify: edge cases trigger expected behaviors.

- [x] **TASK-073** — Apply edge-case error handling for admin auth and pendaftaran.
  Files: `src/app/(auth)/login/page.tsx`, `src/components/admin/pendaftaran/PendaftaranModal.tsx`
  Notes: Per § 12 Admin Auth + Admin Pendaftaran tables. Generic login error. Session-expired handling (redirect to /login on protected action failure). WA number invalid: button disabled + tooltip. Verify: edge cases trigger expected behaviors.

- [x] **TASK-074** — Apply edge-case error handling for article CMS.
  Files: `src/components/admin/artikel/TiptapEditor.tsx`, `src/lib/actions/uploads.ts`, `src/lib/actions/articles.ts`, `src/components/admin/artikel/ArticleEditor.tsx`
  Notes: Per § 12 Article CMS table. Image > 5MB rejected with toast. Unsupported MIME rejected. Storage upload failure surfaced. Slug collision validation. Verify: each edge case triggers expected behavior.

- [x] **TASK-075** — Apply edge-case error handling for testimoni CMS.
  Files: `src/components/admin/testimoni/TestimoniTable.tsx`, `src/components/admin/testimoni/TestimoniModal.tsx`
  Notes: Per § 12 Testimoni CMS table. Drag-reorder rollback on error. Featured toggle rollback. Photo upload failure does not block save. Verify: each edge case triggers expected behavior.

- [x] **TASK-076** — Empty states and skeletons across admin tables.
  Files: `src/components/admin/pendaftaran/PendaftaranTable.tsx`, `src/components/admin/artikel/ArtikelTable.tsx`, `src/components/admin/testimoni/TestimoniTable.tsx`, `src/components/admin/overview/StatCard.tsx`, `src/components/admin/overview/RecentLeadsTable.tsx`
  Notes: Skeleton state during initial load (shadcn Skeleton). Empty state with helpful copy per UI/UX Requirements. Verify: with empty DB, each admin page shows empty-state copy; on load, skeleton renders.

- [x] **TASK-077** — Accessibility pass.
  Files: (all public components)
  Notes: Per § 7 Accessibility. Add `lang="id"` to html (verify TASK-004). Add skip-to-content link in (public)/layout. Ensure all images have alt. Ensure all form inputs have labels and aria-describedby for errors. Test keyboard navigation through landing and registration. Run Lighthouse accessibility audit, target ≥ 95. Verify: Lighthouse score ≥ 95; manual keyboard tab navigation works.

- [x] **TASK-078** — Performance pass.
  Files: (image components, page components)
  Notes: Convert all hero/cover images to optimized formats via Next Image. Set explicit width/height to prevent CLS. Audit bundle size; ensure public JS bundle < 150KB gzipped. Verify LCP < 2.5s on mobile 4G via Lighthouse. ISR configured per FR-008/FR-009. Verify: Lighthouse performance score ≥ 85 on mobile.

- [x] **TASK-079** — Seed launch content: minimum 5 articles, 5 testimonials, hardcoded coach/gallery photos.
  Files: `src/data/coaches.ts`, `src/data/gallery.ts`, `public/images/coaches/*`, `public/images/gallery/*`, `supabase/seed.sql`
  Notes: Replace placeholder photos with real ones (when owner provides). Update coach bios with real data. Seed 5 articles covering: usia ideal, jenis kelas, tanda coach kompeten, persiapan trial pertama, mitos berenang. Seed 5 testimonials. Verify: live site shows real content, looks complete.

- [ ] **TASK-080** — Run cross-device manual QA.
  Files: (none; manual)
  Notes: Test on real iPhone (Safari), real Android (Chrome), desktop Chrome and Safari. Walk through magic moment flow: open article → read → tap CTA → land on jenis kelas section. Walk through registration end-to-end. Test admin in desktop Chrome and Safari. Verify: no critical bugs; all flows work.

- [x] **TASK-081** — Pre-launch security review.
  Files: `supabase/migrations/*`, `src/lib/supabase/admin.ts`, `src/lib/actions/*`
  Notes: Verify RLS policies cover all access patterns (anon, authenticated). Verify service-role key is only imported in `admin.ts` and called from Server Actions / Server Components, never from Client Components. Verify all Server Actions validate input via Zod first. Verify file uploads validate MIME + size on the server. Run Supabase advisor: `mcp__supabase__get_advisors` should report no critical security findings. Verify: advisor passes; manual code review of all admin actions confirms auth-check first.

- [ ] **TASK-082** — Production deploy and post-launch monitoring setup.
  Files: (none; external action)
  Notes: Final merge to main. Verify production URL responds. Submit sitemap to Google Search Console. Verify Vercel Analytics is collecting events. Document the launch in a brief release note (commit message or GitHub release). Monitor for 48 hours: check error logs, lead form submissions, page-load times. Verify: leads submitted on production reach Supabase; admin can log in and manage them.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD and vision doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything.** The PRD and vision doc can be large. Each phase's Reference sections tell the agent exactly what to read.
- **Don't skip tasks.** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase.** Run `npm run dev` and walk through the new functionality before claiming the phase complete.
- **Review before moving on.** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged.
- **Update the status line.** After completing tasks, update the header: `**Status:** X/82 tasks complete` and `**Current Phase:** Phase N`.
- **Bahasa Indonesia for user copy, English for code and docs.** Maintain this discipline throughout.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md and docs/product-vision.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called `phase-{N}/{slug}`, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."

---

## Post-Launch Backlog (placeholder)

Items deferred from MVP scope and explicitly out-of-scope features. Re-prioritize after the first 90 days based on real usage signals.

- Email or WhatsApp automated follow-up
- Multi-template WhatsApp picker in admin pendaftaran modal
- Coach CMS (admin manage coach profile + cert + foto)
- Class schedule CMS / dynamic availability per class
- Online payment (DP, registration fee) via Midtrans or Xendit
- In-app article performance analytics
- Article search (when article count > 30)
- Tag system beyond flat category
- Public testimoni submission with moderation
- Mobile admin (read-only triage)
- Auto-save in article editor
- Lightbox for gallery
- Dark mode
- Multi-language (if expansion beyond Indonesian-speaking audience)
- Multi-branch / multi-location management
